나는 코딩을 모른다. 전문 용어는 쉽고 간결하게 설명한다.
모든 설명·보고는 한국어로 하며, 쉽고 간결하게 한다.

> **WOMC 철학:** 사람은 원하는 것과 되돌릴 수 없는 결정만 맡고, 나머지는 모델이 맡는다. AGENTS.md에는 자율 실행 원칙, 프로젝트 목적·지속 제약·완료 기준, 작업별 읽기 경로, 공통 검증 방법만 둔다.
<!-- womc:version=1.5.0 -->
<!-- womc:project-harness:start -->
<!-- womc:context-snapshot=sha256:5448ad62ccd525aa104d7b94f50526af1f0f0e154777307c565606c041200ca7 -->
## WOMC 프로젝트 하네스

### 자율 실행 원칙
- 목표와 범위가 충분히 분명하면 조사나 계획에서 멈추지 않고 구현·검증·결과 보고까지 진행한다.
- 결과를 크게 바꾸지 않는 세부 사항은 프로젝트 관례와 증거를 바탕으로 정하고, 되돌리기 어려운 결정이나 목표를 바꾸는 선택만 사용자에게 묻는다.
- 필요한 질문은 현재 환경에 비동기 질문 도구가 있으면 활용하고, 답변과 무관한 안전한 작업은 계속한다. 답변이나 승인이 필요한 작업은 응답을 기다리며, 무응답을 동의로 간주하지 않는다.
- 작업 중 이후 결과를 바꾸는 프로젝트 사실·제약·완료 기준·검증 방법을 알게 되면 현재 작업을 끝내기 전에 하네스나 연결된 문서·스킬에 반영한다.
- 현재 작업의 진행 기록과 일회성 계획은 하네스에 쌓지 않는다.
- 스킬을 만들거나 바꿀 때는 기존 지침으로 해결되지 않는 재사용 가치가 있는지 확인하고, 필요한 작업에서만 읽도록 한다. 일반적인 작업 조언만 담은 스킬은 만들지 않는다.

### 서브에이전트 위임

- 메인 모델·추론 수준은 사용자가 선택한다. 아래 기준은 서브에이전트에만 적용하며, 사용자 지정 모델·위임 제한·예산·승인과 프로젝트 제한이 우선한다. 이 정책은 외부 판단 엔진·API·훅·분류기·상태 저장 없이 매 요청마다 하는 일회성 내부 자연어 판단이며, 판단·계획·작업 기록을 `AGENTS.md`에 누적하지 않는다.
- 요청 전체는 `readiness=ready|partial|blocked`, 각 작업은 `status=ready|blocked`로만 정리한다. `ready|partial`은 실행 가능한 일이 있을 때 `planned_execution=direct|mixed`, `blocked`는 `none`을 쓰며, `mixed`에는 준비된 위임과 독립적인 메인 병행 작업이 모두 있어야 한다. 이 값은 호출 성공·실행·결과의 증거가 아니며, 현재 환경에는 순수 위임(`pure delegate`)이 없다. 메인이 언제나 통합·최종 검증·보고 책임을 진다.
- `blocked`는 막힌 범위, 자연어 이유, `needed_to_unblock`을 함께 적고 사용자 입력/승인·정책 금지·근거 부족·도구 접근·미지원 중 원인을 구분한다. 현재 권한과 도구로 범위 안에서 바로 해소할 선행 확인은 차단이 아니라 `ready` 작업의 실행 순서다. `partial`은 막힌 작업과 결과·의존성이 독립이고 사용자의 선택지를 좁히지 않는 되돌릴 수 있는 안전한 로컬 작업만 계속할 때 쓴다.
- 사용자는 적극 위임을 선호한다. 메인은 분리 가능한 조사·구현·검토와 병행할 유용한 일을 먼저 찾되, 짧은 단일 작업·전달 비용이 더 큰 일·병행 이득이 없는 일은 직접 한다. 작업명·대화 길이·파일 수가 아니라 모호성·오류 영향·검증 난도로 판단한다.

| 위임 작업의 성격 | 모델 | 추론 수준 |
| --- | --- | --- |
| 기준이 명확한 추출·분류·파일 위치 탐색 | `gpt-5.6-luna` | `low` |
| 질문과 출처 범위가 좁은 웹 조사·공식 문서 사실 확인 | `gpt-5.6-luna` | `medium` |
| 여러 출처 비교·코드 경로 추적·독립적인 일반 구현과 테스트 | `gpt-5.6-terra` | `medium` |
| 상충하는 근거 해석·복잡한 원인 분석·보안 및 중요한 변경 검토 | `gpt-5.6-sol` | `high` |
| 여러 영역에 걸친 모호한 설계로 별도의 깊은 판단이 꼭 필요한 작업 | `gpt-6-astra` | `high` |

- 실제 지원 조합·도구 접근을 확인하고 모델과 추론 수준을 호출 인수로 함께 지정한다. 고정 모델인 사용자 정의 역할이 선택과 충돌하면 쓰지 않는다. 미지원이면 허용 조합으로 바꾸거나 메인이 직접 맡고, 모델 상속·계획을 성공으로 보거나 예산·설정·권한을 우회하지 않는다.
- 위임에는 목표·완료 기준·루트·읽기/수정/제외 범위·적용 `AGENTS.md`/스킬 경로·확인/미확인 사실·대화 전용 제약(승인·허용 모델·예산·동시 인원·네트워크·푸시)·도구·모델/추론·반환할 근거/변경/검증을 정확히 전달하고 무관한 정보는 제외한다. 담당·모델·추론·이유를 사용자에게 짧게 알리고, 담당 범위를 겹치지 않게 하며 동시 한도가 차면 완료 후 배정한다. 재위임을 따로 허용하지 않으면 서브에이전트가 직접 수행하게 하고 결과의 근거·변경·검증·미확인을 검토한다.
- 권한·정책·확정된 미지원은 재시도 0회, 일시 도구 오류는 1회, 근거 누락 보완은 1회만 한다. 추론 부족이면 미해결 범위만 1회 다시 판단해 재배정하며, 숫자 신뢰도나 점수로 판단을 꾸미지 않는다.

### 프로젝트 목적
새·기존 프로젝트에 얇은 Codex AGENTS.md 하네스를 자연어 요청으로 설치·갱신해, 사용자가 목표와 되돌리기 어려운 결정에 집중하도록 하는 플러그인이다.

### 변하지 않는 제품 원칙
- 프로젝트 관련 설명과 지침 문서는 한국어로 작성한다.
- WOMC 버전은 날짜·시간이나 빌드 메타데이터를 붙이지 않은 x.x.x 형식만 사용한다.

### 지속적인 완료 기준
- 하네스는 프로젝트별 지속 맥락과 필요한 읽기 경로를 유지하면서 일회성 작업 기록을 누적하지 않는다.
- 새 프로젝트 생성과 기존 프로젝트 갱신은 사용자 작성 내용을 보존하고 반복 실행해도 같은 결과를 낸다.

### 보안·승인 경계
- 비밀 정보 열람·노출, 데이터 삭제, 운영 데이터 변경, 실제 결제, 배포, 외부 서비스 변경, 원격 저장소 변경 전에는 명시적 승인을 받는다.
- 되돌릴 수 있는 일반 프로젝트 수정과 안전한 로컬 검증은 별도 승인 없이 진행할 수 있다.

### 작업별 문서·스킬 경로
- 프로젝트 목적·지속 제약·완료 기준·읽기 경로·워크스페이스·검증 명령을 바꾼 작업은 끝내기 전에 `$works-on-my-codex`로 루트 `AGENTS.md` 하네스를 갱신한다.
- WOMC 설치·온보딩·수동 갱신 또는 사용자 경험 변경: `docs/사용자-경험.md` 문서를 읽는다. <!-- womc:manual-route -->
- 프로젝트 맥락 또는 동작 변경: `README.md` 문서를 읽는다.

### 공통 검증 방법
- 변경의 영향과 위험에 맞는 최소 충분 검증을 선택하고, 아래 명령은 해당 범위에 필요할 때 실행한다:
  - `python3 -B -m unittest discover -s tests -v` (프로젝트 검증)
- 검증 명령이 성공 종료해야 통과로 본다. 실행할 수 없다면 구체적인 방해 요인을 보고하고 통과했다고 표현하지 않는다.
<!-- womc:project-harness:end -->
