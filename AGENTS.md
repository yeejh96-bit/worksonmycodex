나는 코딩을 모른다. 전문 용어는 쉽고 간결하게 설명한다.
모든 설명·보고는 한국어로 하며, 쉽고 간결하게 한다.

> **WOMC 철학:** 사람은 원하는 것과 되돌릴 수 없는 결정만 맡고, 나머지는 모델이 맡는다. AGENTS.md에는 자율 실행 원칙, 프로젝트 목적·지속 제약·완료 기준, 작업별 읽기 경로, 공통 검증 방법만 둔다.
<!-- womc:version=1.5.2 -->
<!-- womc:project-harness:start -->
<!-- womc:context-snapshot=sha256:5448ad62ccd525aa104d7b94f50526af1f0f0e154777307c565606c041200ca7 -->
## WOMC 프로젝트 하네스

### 자율 실행 원칙
- 목표와 범위가 충분히 분명하면 조사나 계획에서 멈추지 않고 구현·검증·결과 보고까지 진행한다.
- 결과를 크게 바꾸지 않는 세부 사항은 프로젝트 관례와 증거를 바탕으로 정하고, 되돌리기 어려운 결정이나 목표를 바꾸는 선택만 사용자에게 묻는다.
- 필요한 질문은 현재 환경에 비동기 질문 도구가 있으면 활용하고, 답변과 무관한 안전한 작업은 계속한다. 답변이나 승인이 필요한 작업은 응답을 기다리며, 무응답을 동의로 간주하지 않는다.
- 작업 중 이후 결과를 바꾸는 프로젝트 사실·제약·완료 기준·검증 방법을 알게 되면 현재 작업을 끝내기 전에 하네스나 연결된 문서·스킬에 반영한다.
- 현재 작업의 진행 기록과 일회성 계획은 하네스에 쌓지 않는다.
- 스킬을 만들거나 바꿀 때는 기존 지침으로 해결되지 않는 재사용 가치가 있는지 확인하고, 필요한 작업에서만 읽도록 한다. 일반적인 작업 조언만 담은 스킬은 만들지 않는다.

### 서브에이전트 위임

- 메인 모델·추론 수준은 사용자가 선택한다. 아래 기준은 서브에이전트에만 적용하며, 사용자가 정한 모델·위임 제한·예산을 우선한다.
- 사용자는 서브에이전트의 적극적인 활용을 선호한다. 메인은 작업마다 분리 가능한 조사·구현·검토를 먼저 찾고, 범위와 완료 기준이 분명하며 다른 유용한 일을 병행할 수 있으면 직접 모두 처리하기보다 위임을 우선한다. 전체 요청이나 대화가 길어도 필요한 맥락을 독립 범위로 나눌 수 있으면 위임한다. 짧은 단일 작업, 맥락을 분리하기 어려워 전달 비용이 이득보다 큰 작업, 결과를 기다리는 동안 병행할 유용한 일이 없는 작업은 직접 처리한다.
- 작업 이름보다 모호성·오류 영향·검증 난도를 보고 아래 역할과 추론 수준을 선택한다. 웹 조사라는 이유만으로 낮은 수준을 고르거나, 모든 일을 최고 수준에 맡기지 않는다.

| 위임 작업의 성격 | 필요한 모델의 역할 | 추론 수준 |
| --- | --- | --- |
| 기준이 명확한 추출·분류·파일 위치 탐색 | 빠르고 효율적인 모델 | `low` |
| 질문과 출처 범위가 좁은 웹 조사·공식 문서 사실 확인 | 빠르고 효율적인 모델 | `medium` |
| 여러 출처 비교·코드 경로 추적·독립적인 일반 구현과 테스트 | 일반 코딩·분석에 적합한 균형형 모델 | `medium` |
| 상충하는 근거 해석·복잡한 원인 분석·보안 및 중요한 변경 검토 | 복잡한 추론·코딩에 강한 모델 | `high` |
| 여러 영역에 걸친 모호한 설계로 별도의 깊은 판단이 꼭 필요한 작업 | 현재 제공되는 최상위 추론 모델 | `high` |

- 매 위임 전에 현재 도구가 제공하는 모델 목록·설명, 지원 추론 수준과 필요한 도구 접근을 확인한다. 사용자 제한을 먼저 적용하고, 호출 가능한 범용 GPT 모델 중 번호가 가장 높은 세대(GPT-N)를 고른다. 그 세대 안에서 위 표의 역할에 맞는 모델을 선택한다. 해당 역할의 모델이 없으면 과제를 감당할 수 있는 같은 세대의 다른 모델을 고른다. 같은 세대에서 필요한 도구·추론 조합을 지원하지 않으면 메인이 직접 수행하거나 제한을 보고한다. 사용자 지정이 없는 한 이전 세대 모델로 자동 대체하지 않는다. 실제 가격이 확인되지 않으면 비용 우위를 주장하지 않는다. 설명만으로 역할을 구분할 수 없으면 접근 가능한 공식 최신 모델 안내를 확인한다. 모델명을 정책에 고정하거나 출시 때마다 표를 갱신하지 않는다.
- 선택한 모델의 실제 식별자와 지원되는 추론 수준을 호출 인수로 지정한다. 프롬프트에 이름만 쓰는 것은 모델 선택이 아니다. 사용자 정의 에이전트의 고정 설정과 충돌하면 그 역할을 쓰지 않는다. 도구가 모델 변경과 전체 대화 상속을 함께 지원하지 않으면 필요한 맥락만 전달한다.
- 요청에는 목표·완료 기준, 프로젝트 루트·담당 파일과 읽기/수정 범위, 필요한 스킬의 이름·진입 경로, 확인된 사실·미확인 사항을 담는다. 대화에만 있는 사용자 제한·예산·승인 조건도 명시적으로 전달한다. 서브에이전트도 적용되는 프로젝트 지침과 해당 스킬을 읽고 따른다. 스킬·필수 도구에 접근할 수 없으면 추측하지 않고 메인에 알린다.
- 독립 범위와 병렬화 실익에 맞는 인원을 배정하고 사용자·실행 환경의 동시 한도를 따른다. 한도가 차면 새 작업은 기존 작업 완료 후 배정한다. 같은 일을 중복 수행하거나 같은 파일을 동시에 수정하지 않도록 담당을 나누고, 공통 연결 파일과 통합은 메인 또는 한 담당자가 맡는다. 서브에이전트는 재위임이 별도로 배정되지 않으면 직접 수행한다. 기존 승인 경계는 그대로 적용한다.
- 근거·테스트·미확인 사항을 결과에 포함한다. 근거 부족이면 질문을 좁히고, 추론 난도가 원인이면 해당 미해결 부분만 적합한 상위 조합으로 재배정한다. 권한·도구·접속 실패는 모델을 올려 해결하려 하지 않는다. 같은 원인으로 반복 위임하지 않는다.
- 지원되지 않는 조합은 선택한 최신 세대 안에서 요구 품질을 충족하는 허용된 조합으로 바꾸고 이유를 알린다. 적합한 대안이나 모델 지정 기능이 없으면 메인이 가능한 부분을 직접 수행한다. 메인도 필수 근거·도구에 접근할 수 없으면 해당 부분을 보류하고 미확인 범위와 필요한 입력을 보고한다. 모델 상속을 지정 성공으로 보고하거나 예산·설정·권한을 우회하지 않는다.
- 메인은 위임 때 담당·모델·추론 수준·이유를 짧게 알리고, 반환된 근거와 변경분을 검토해 통합·최종 검증·보고를 책임진다. 작은 작업에 위임 판단표를 매번 출력하지 않는다.

### 프로젝트 목적
새·기존 프로젝트에 얇은 Codex AGENTS.md 하네스를 자연어 요청으로 설치·갱신해, 사용자가 목표와 되돌리기 어려운 결정에 집중하도록 하는 플러그인이다.

### 변하지 않는 제품 원칙
- 프로젝트 관련 설명과 지침 문서는 한국어로 작성한다.
- WOMC 버전은 날짜·시간이나 빌드 메타데이터를 붙이지 않은 x.x.x 형식만 사용한다.

### 지속적인 완료 기준
- 하네스는 프로젝트별 지속 맥락과 필요한 읽기 경로를 유지하면서 일회성 작업 기록을 누적하지 않는다.
- 새 프로젝트 생성과 기존 프로젝트 갱신은 사용자 작성 내용을 보존하고 반복 실행해도 같은 결과를 낸다.

### 보안·승인 경계
- 비밀 정보 열람·노출, 데이터 삭제, 운영 데이터 변경, 실제 결제, 배포, 외부 서비스 변경, 원격 저장소 변경 전에는 명시적 승인을 받는다.
- 되돌릴 수 있는 일반 프로젝트 수정과 안전한 로컬 검증은 별도 승인 없이 진행할 수 있다.

### 작업별 문서·스킬 경로
- 프로젝트 목적·지속 제약·완료 기준·읽기 경로·워크스페이스·검증 명령을 바꾼 작업은 끝내기 전에 `$works-on-my-codex`로 루트 `AGENTS.md` 하네스를 갱신한다.
- WOMC 설치·온보딩·수동 갱신 또는 사용자 경험 변경: `docs/사용자-경험.md` 문서를 읽는다. <!-- womc:manual-route -->
- 프로젝트 맥락 또는 동작 변경: `README.md` 문서를 읽는다.

### 공통 검증 방법
- 변경의 영향과 위험에 맞는 최소 충분 검증을 선택하고, 아래 명령은 해당 범위에 필요할 때 실행한다:
  - `python3 -B -m unittest discover -s tests -v` (프로젝트 검증)
- 검증 명령이 성공 종료해야 통과로 본다. 실행할 수 없다면 구체적인 방해 요인을 보고하고 통과했다고 표현하지 않는다.
<!-- womc:project-harness:end -->
