# WOMC 사용자 경험

## 제품 약속

WOMC는 사용자가 만들고 싶은 것과 되돌리기 어려운 결정에 집중하도록 한다. 사용자가 자연어로 적용이나 업데이트를 요청하면 Codex는 프로젝트를 읽고 필요한 하네스를 만들며, 작업하면서 알게 된 지속 정보를 다음 작업에 남긴다. 사용자는 WOMC의 내부 명령이나 에이전트 설정을 직접 관리하지 않는다.

## 대표 사용자 페르소나

### 민지 — 제품은 만들고 싶지만 에이전트 설정은 관리하고 싶지 않은 사람

- 만들고 싶은 제품과 사용자 문제는 설명할 수 있다.
- 저장소 구조, 테스트 명령, `AGENTS.md`, 스킬의 차이는 잘 모르거나 매번 기억하고 싶지 않다.
- 설치할 때 한 번 정도의 마켓플레이스 등록과 플러그인 설치는 받아들일 수 있다.
- 일상 작업에서는 Codex가 합리적인 세부 결정을 내리고 구현과 검증까지 끝내기를 기대한다.
- 삭제, 배포, 결제, 운영 데이터 변경처럼 되돌리기 어려운 결정만 직접 확인하고 싶다.
- 새 문서나 스킬을 만들 때마다 `AGENTS.md`를 손으로 동기화하고 싶지 않다.

민지의 성공 기준은 “Codex를 열고 원하는 기능을 말하면, 지난 작업에서 정해진 프로젝트 맥락과 필요한 문서를 이미 알고 끝까지 작업하는 것”이다.

## 경험 원칙

1. 세션 시작 자동화는 사용하지 않고, 적용이나 업데이트가 필요할 때 자연어로 WOMC를 호출한다.
2. 프로젝트에 이미 있는 코드와 문서로 알 수 있는 것은 사용자에게 묻지 않는다.
3. 목표와 범위가 충분하면 Codex가 조사·계획·구현·검증·보고를 이어서 수행한다.
4. `AGENTS.md`에는 다음 작업의 결과를 바꾸는 지속 정보만 둔다.
5. 긴 배경은 `docs/`, 기존 지침으로 해결되지 않는 재사용 가능한 고유 절차·도구는 스킬, 경로별 제약은 가장 가까운 중첩 `AGENTS.md`에 둔다. 스킬은 필요한 작업에서만 읽고 자동 발견되는 설명을 루트에 중복하지 않는다.
6. 현재 작업의 계획·진행 상황·완료 기록은 하네스에 쌓지 않는다.
7. 되돌리기 어려운 결정과 프로젝트 목표를 바꾸는 선택만 사용자에게 묻는다.
8. 필요한 질문은 지원되는 환경에서 비동기로 전달한다. 답변과 무관한 안전한 작업은 계속하고, 답변이나 승인이 필요한 작업만 응답을 기다린다. 무응답은 동의가 아니다.
9. 메인 모델과 추론 수준은 사용자가 고른다. 메인은 분리 가능한 조사·구현·검토를 먼저 찾아 적극적으로 위임하고, 작업마다 적절한 모델과 추론 수준을 지정한다. 사용자가 매번 수동으로 모델을 바꾸도록 요구하지 않는다. 개별 위임 금지·예산·환경 제한은 이 선호보다 우선한다.

## 사용자 흐름

### 1. Git 마켓플레이스 등록과 설치

민지는 한 번만 Git 마켓플레이스를 등록한다.

```sh
codex plugin marketplace add yeejh96-bit/worksonmycodex --ref main
```

그다음 `/plugins`에서 `WOMC`를 검색해 설치한다.

기대 결과: 세션 시작 훅이나 자동 점검 없이 WOMC 스킬을 자연어로 사용할 수 있다.

### 2. 첫 프로젝트에 적용

민지가 프로젝트 루트에서 Codex를 시작하고 `이 프로젝트에 WOMC를 적용해 줘`라고 요청한다.

- 하네스가 없으면 모델이 현재 프로젝트를 조사해 WOMC를 적용한다.
- 기존 프로젝트면 README, 지속 문서, 매니페스트, 로컬 스킬과 검증 설정을 조사한다. 운영 스킬별 고유 가치와 사용 조건을 확인해 유지·축소·통합·문서 이동·제거 후보를 판단하되, 실제 삭제는 승인 경계를 따른다.
- 새 프로젝트는 추가 스킬 0개로 시작해도 정상이다. 반복된다는 이유만으로 계획·코딩·테스트 같은 일반 스킬을 만들지 않는다. 참고·백업 폴더의 과거 스킬과 다른 프로젝트의 지침은 가져오지 않는다.
- 빈 프로젝트라서 목적을 알 수 없을 때만 무엇을 만들지 묻는다.
- 비어 있지 않은 `AGENTS.override.md`나 깨진 WOMC 마커가 있으면 덮어쓰지 않고 충돌을 설명한다.

기대 결과: 루트 `AGENTS.md`에 자율 실행 원칙과 프로젝트 브리핑이 생긴다.

### 3. 프로젝트 브리핑 형성

WOMC는 코드만으로 다음 작업에 충분하지 않은 정보를 골라 기록한다.

- 프로젝트 목적: 무엇을 누구를 위해 만드는지 한 문장
- 변하지 않는 제품 원칙과 보안·승인 경계
- 여러 작업에 계속 적용되는 완료 기준
- 작업 상황별로 먼저 읽을 문서와 사용할 스킬
- 실제로 실행할 수 있는 공통 검증 명령

예시는 다음과 같다.

```markdown
### 프로젝트 목적
팀이 로컬 문서를 안전하게 정리하고 검색하도록 돕는 앱이다.

### 작업별 문서·스킬 경로
- 모든 구현 작업 전에 `docs/product-principles.md`를 읽는다.
- 결제 정책이나 요금제를 변경할 때는 `docs/billing.md`를 읽는다.
- 릴리스 후보를 만들거나 배포 전 검증을 할 때는 `skills/release-helper/SKILL.md`를 읽고 스킬을 따른다.
```

기대 결과: 긴 내용을 매번 프롬프트에 넣지 않아도 Codex가 상황에 맞는 진입 경로를 안다.

### 4. 일상적인 기능 작업

민지는 “검색 결과를 태그로 필터링하게 해줘”처럼 결과만 요청한다. Codex는 시작할 때 읽은 `AGENTS.md`에 따라 관련 문서와 스킬을 불러오고, 안전한 범위에서는 추가 승인 없이 구현과 검증을 마친다.

메인은 작업을 살펴보고 직접 처리할 부분과 독립적으로 위임할 부분을 정한다. 위임할 때 현재 도구가 지원하는 가장 최신 범용 GPT 세대를 먼저 고른다. 그 세대 안에서 공개 문서의 특정 옵션 확인에는 빠른 모델, 여러 코드 경로의 비교에는 균형형 모델을 선택할 수 있다. 작업이 작거나 결과 대기 때문에 병행할 일이 없으면 직접 처리한다. 어려운 분석·검토에는 같은 세대의 더 강한 조합을 선택한다. 기준표는 [위임 정책 원본](../skills/works-on-my-codex/assets/delegation-policy.md)에서 관리한다.

서브에이전트에는 담당 범위와 필요한 프로젝트 스킬·자료 경로를 전달한다. 메인은 담당·모델·추론 수준과 위임 이유를 짧게 알리고, 근거를 검토해 최종 결과를 책임진다. 지원되지 않는 모델이나 도구는 제한을 알리고 대안을 선택한다. WOMC는 메인 모델이나 전역 설정을 바꾸지 않으며, 위임 금지·모델 제한 등 사용자 지정을 우선한다.

기대 결과: 사용자는 하네스를 운영하는 대신 제품 결정을 내린다.

### 5. 프로젝트가 배우고 변하는 과정

작업 중 다음 변화가 생기면 같은 작업을 끝내기 전에 하네스를 갱신한다.

- 다음 작업에도 영향을 주는 제품 사실이나 제약을 새로 알게 됨
- 프로젝트 수준 완료 기준이나 검증 명령이 바뀜
- 기존 지침으로 해결되지 않는 재사용 가치가 확인된 절차·도구를 로컬 스킬로 만듦
- 반드시 읽어야 하는 긴 근거 문서를 추가하거나 역할을 바꿈
- 새 앱·패키지·서비스 같은 워크스페이스 영역을 추가함

Codex 밖에서 읽기 경로, 비표준 로컬 스킬, 워크스페이스, 검증 명령의 구조가 바뀐 경우에는 `WOMC 업데이트해 줘`라고 요청해 하네스를 다시 맞춘다. 일반 문서 본문 수정은 갱신 대상이 아니며, 작업 중 새로 알게 된 지속 정보는 현재 작업에서 반영한다.

일반 소스 코드 수정은 하네스 갱신을 일으키지 않는다. 작업 중에는 만들거나 바꾸는 스킬만 필요성을 확인하고, 전체 스킬 점검은 처음 적용·명시적 갱신·정리 요청에서 수행한다.

기대 결과: `AGENTS.md`는 고정된 초기 파일이 아니라 프로젝트와 함께 다듬어지는 짧은 브리핑이 된다.

### 6. 플러그인 갱신

민지가 “이 프로젝트 WOMC를 최신 버전으로 업데이트해 줘”처럼 실행을 요청하면 WOMC는 Git 마켓플레이스 스냅샷을 갱신하고 플러그인을 다시 추가해 설치본을 최신화한다. 이 요청은 두 로컬 Codex 설정 변경에 대한 승인으로 보며 같은 작업을 다시 확인하지 않는다. 버전이나 업데이트 방법을 질문만 한 경우에는 실행하지 않는다.

설치된 버전이 `x.x.x` 형식이고 활성화됐는지 확인한 뒤, 최신 스킬로 현재 프로젝트의 `AGENTS.md` 하네스를 갱신한다. 프로젝트 목적·지속 제약·완료 기준·수동 읽기 경로와 검증 명령은 보존한다. 단계가 실패하면 성공으로 표현하거나 무한 재시도하지 않고 구체적인 오류를 보고한다.

갱신 뒤 같은 생성기의 `--check`로 하네스를 확인한다. 결과는 설치본, 프로젝트 하네스, 실행 중인 세션의 자동 반영으로 구분한다. 세션의 스킬 목록 갱신을 실제로 검사하지 않았다면 미검증이라고 알린다. 최신 스킬 파일을 직접 읽는 것과 세션의 자동 갱신은 같은 검사가 아니다. 동일 세션 검증이 필요한 경우에만 스킬의 선택형 수동 통합 검사 절차를 사용한다.

기대 결과: 사용자는 마켓플레이스와 플러그인 명령을 따로 기억하지 않고 자연어 요청 한 번으로 설치본과 현재 프로젝트 하네스를 함께 최신화한다.

## [`works-on-my-claude`](https://github.com/yeejh96-bit/works-on-my-claude)에서 가져온 것과 Codex에 맞춘 것

가져오는 핵심은 프로젝트 브리핑, 작업 중 알게 된 지속 정보의 자율적인 반영, 긴 근거와 짧은 진입 지침의 분리다. 설치와 갱신은 세션 훅 대신 사용자의 자연어 요청으로 시작한다.

Codex에서는 다음처럼 적용한다.

- `CLAUDE.md` 대신 [Codex가 작업 전에 읽는 `AGENTS.md`](https://learn.chatgpt.com/ko-KR/docs/agent-configuration/agents-md)를 사용한다.
- `.claude/rules` 대신 적용 경로에 가장 가까운 중첩 `AGENTS.md`를 사용한다.
- 권한 파일을 별도로 생성하지 않고 현재 Codex 샌드박스와 승인 정책을 따른다.
- 모든 작업에 고정 단계, 체크포인트 커밋, 별도 서브에이전트 검증을 강제하지 않는다.
- 목표가 명확하면 Codex가 현재 환경과 작업 위험에 맞는 방법을 선택해 완료까지 지속하도록 한다.
- 현재 계획과 진행 기록은 `AGENTS.md`에 저장하지 않는다. 장기 작업 상태는 Codex의 작업·목표 기능이나 프로젝트의 기존 작업 관리 방식을 사용한다.

## 출시 확인 기준

- 새 프로젝트에서 WOMC 적용을 요청하면 루트 `AGENTS.md`가 생성되며 기본 스킬 묶음을 추가하지 않는다.
- 위임 정책은 새 생성과 기존 갱신에 포함되며, 메인 모델·추론 수준·기존 에이전트 설정은 변경하지 않는다.
- 위임 판단의 모의 검증과 실제 모델 호출·스킬 사용 검증을 구분해 보고한다.
- 스킬 점검은 고유 지식·도구·품질 기준과 사용 조건을 근거로 하며, `--check` 통과를 효용 판정으로 보고하지 않는다.
- 생성·갱신한 `AGENTS.md`의 첫 두 줄은 “나는 코딩을 모른다. 전문 용어는 쉽고 간결하게 설명한다.”와 “모든 설명·보고는 한국어로 하며, 쉽고 간결하게 한다.”로 고정하고, 반복 갱신해도 중복하지 않는다.
- 기존 프로젝트에서는 코드로 알 수 있는 사실 때문에 불필요하게 질문하지 않는다.
- Codex가 자동 발견하는 스킬의 `description`은 `AGENTS.md`에 중복되지 않고, 비표준 스킬만 짧은 사용 조건과 진입 경로로 기록된다.
- 긴 근거 문서는 전역 또는 작업별 조건과 함께 읽기 경로로 기록된다.
- WOMC 업데이트를 요청하면 읽기 경로·워크스페이스·검증 명령의 현재 구조가 하네스에 반영된다.
- 필요한 질문을 전달한 뒤 답변과 무관한 안전한 작업은 계속하며, 답변이나 승인이 필요한 변경을 추정으로 진행하지 않는다.
- 업데이트 결과는 설치본·하네스·세션 자동 반영을 구분하고, 검사하지 않은 항목을 통과로 보고하지 않는다.
- 일반 문서 본문 수정만으로는 WOMC 갱신이 발동하지 않는다.
- 일반 소스 코드 변경만으로 `AGENTS.md`가 바뀌지 않는다.
- 수동으로 남긴 프로젝트 목적·원칙·승인 경계·완료 기준·읽기 경로·검증 명령이 갱신에서 보존된다.
- 회의록·구현 일지·변경 기록이 지속 지침으로 누적되지 않는다.
