interface:
  display_name: "WOMC Status Line"
  short_description: "Show model, project, context, and account limits"
  default_prompt: "Use $works-on-my-codex-statusline to configure the compact WOMC status line."
policy:
  allow_implicit_invocation: true
